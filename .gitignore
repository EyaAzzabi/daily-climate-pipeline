# Raw landing zone is regenerated by the pipeline
data/bronze/

# The warehouse is a build artifact, rebuilt from data/fact_daily_weather.csv.
# It does not compress: committing it daily would add ~1.6 GB of history a year.
data/*.duckdb
data/*.duckdb.wal

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Secrets
.env
